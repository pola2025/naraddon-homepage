---
title: 심사관 이미지 업로드 UX 개선
날짜: 2025-10-18
프로젝트: 나라똔
카테고리: 기능개발
기능범주: 심사관관리
상태: 완료
구현파일:
  - app/admin/examiners/page.tsx
  - components/ImageUploader.tsx
  - app/api/upload-image/route.ts
관련API:
  - POST /api/upload-image
테스트완료: true
tags:
  - 프로젝트/나라똔
  - 기능/심사관/이미지업로드
  - 기능/파일관리
  - 작업유형/신규기능
  - 작업유형/UX개선
  - 상태/완료
  - 기술/Cloudflare-R2
  - 기술/Next.js
---

#나라똔 #심사관 #이미지업로드 #신규기능 #UX개선 #Cloudflare-R2 #완료

# 심사관 이미지 업로드 UX 개선

## 📋 기능 요약
- **무엇을 만들었는가**: 심사관 이미지 업로드 시 실시간 미리보기, 진행 상태 표시, 에러 처리 개선
- **왜 필요한가**: 기존에는 업로드 후에야 이미지를 확인할 수 있어 잘못된 이미지 업로드 시 재작업 필요
- **어떻게 동작하는가**: 파일 선택 즉시 미리보기 표시, 업로드 진행률 표시, 완료/실패 명확히 안내

## 💻 구현 상세

### 주요 파일
- `app/admin/examiners/page.tsx`: 심사관 목록 페이지, ImageUploader 컴포넌트 통합
- `components/ImageUploader.tsx`: 재사용 가능한 이미지 업로더 컴포넌트
- `app/api/upload-image/route.ts`: Cloudflare R2 업로드 API 엔드포인트

### 핵심 코드

#### 1. ImageUploader 컴포넌트
```typescript
// components/ImageUploader.tsx
'use client';

import { useState } from 'react';
import Image from 'next/image';

export default function ImageUploader({ onUploadComplete }: {
  onUploadComplete: (url: string) => void
}) {
  const [preview, setPreview] = useState<string | null>(null);
  const [uploading, setUploading] = useState(false);
  const [progress, setProgress] = useState(0);
  const [error, setError] = useState<string | null>(null);

  const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0];
    if (!file) return;

    // 파일 크기 검증 (5MB)
    if (file.size > 5 * 1024 * 1024) {
      setError('파일 크기는 5MB 이하여야 합니다');
      return;
    }

    // 파일 타입 검증
    if (!file.type.startsWith('image/')) {
      setError('이미지 파일만 업로드 가능합니다');
      return;
    }

    // 미리보기 생성
    const reader = new FileReader();
    reader.onloadend = () => {
      setPreview(reader.result as string);
    };
    reader.readAsDataURL(file);
  };

  const handleUpload = async () => {
    const fileInput = document.querySelector('input[type="file"]') as HTMLInputElement;
    const file = fileInput?.files?.[0];
    if (!file) return;

    setUploading(true);
    setError(null);
    setProgress(0);

    try {
      const formData = new FormData();
      formData.append('file', file);

      const xhr = new XMLHttpRequest();

      // 진행률 모니터링
      xhr.upload.addEventListener('progress', (e) => {
        if (e.lengthComputable) {
          const percent = Math.round((e.loaded / e.total) * 100);
          setProgress(percent);
        }
      });

      // 업로드 완료
      xhr.addEventListener('load', () => {
        if (xhr.status === 200) {
          const response = JSON.parse(xhr.responseText);
          onUploadComplete(response.url);
          setPreview(null);
        } else {
          setError('업로드 실패: ' + xhr.statusText);
        }
        setUploading(false);
      });

      xhr.addEventListener('error', () => {
        setError('네트워크 에러가 발생했습니다');
        setUploading(false);
      });

      xhr.open('POST', '/api/upload-image');
      xhr.send(formData);
    } catch (error) {
      setError('업로드 중 오류가 발생했습니다');
      setUploading(false);
    }
  };

  return (
    <div className="space-y-4">
      {/* 파일 선택 */}
      <input
        type="file"
        accept="image/*"
        onChange={handleFileChange}
        disabled={uploading}
        className="block w-full"
      />

      {/* 미리보기 */}
      {preview && (
        <div className="relative w-32 h-32 border rounded">
          <Image
            src={preview}
            alt="미리보기"
            fill
            className="object-cover rounded"
          />
        </div>
      )}

      {/* 진행 상태 */}
      {uploading && (
        <div className="space-y-2">
          <div className="w-full bg-gray-200 rounded-full h-2">
            <div
              className="bg-blue-600 h-2 rounded-full transition-all"
              style={{ width: `${progress}%` }}
            />
          </div>
          <p className="text-sm text-gray-600">{progress}% 업로드 중...</p>
        </div>
      )}

      {/* 에러 메시지 */}
      {error && (
        <p className="text-sm text-red-600">{error}</p>
      )}

      {/* 업로드 버튼 */}
      {preview && !uploading && (
        <button
          onClick={handleUpload}
          className="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700"
        >
          업로드
        </button>
      )}
    </div>
  );
}
```

#### 2. API 엔드포인트
```typescript
// app/api/upload-image/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3';

const s3Client = new S3Client({
  region: 'auto',
  endpoint: process.env.CLOUDFLARE_R2_ENDPOINT,
  credentials: {
    accessKeyId: process.env.CLOUDFLARE_R2_ACCESS_KEY_ID!,
    secretAccessKey: process.env.CLOUDFLARE_R2_SECRET_ACCESS_KEY!,
  },
});

export async function POST(request: NextRequest) {
  try {
    const formData = await request.formData();
    const file = formData.get('file') as File;

    if (!file) {
      return NextResponse.json(
        { error: '파일이 없습니다' },
        { status: 400 }
      );
    }

    // 파일명 생성 (UUID + 원본 확장자)
    const ext = file.name.split('.').pop();
    const filename = `examiners/${Date.now()}-${crypto.randomUUID()}.${ext}`;

    // Cloudflare R2에 업로드
    const buffer = Buffer.from(await file.arrayBuffer());

    await s3Client.send(
      new PutObjectCommand({
        Bucket: process.env.CLOUDFLARE_R2_BUCKET_NAME,
        Key: filename,
        Body: buffer,
        ContentType: file.type,
      })
    );

    // 공개 URL 반환
    const publicUrl = `${process.env.CLOUDFLARE_R2_PUBLIC_URL}/${filename}`;

    return NextResponse.json({ url: publicUrl });
  } catch (error) {
    console.error('Upload error:', error);
    return NextResponse.json(
      { error: '업로드 중 오류가 발생했습니다' },
      { status: 500 }
    );
  }
}
```

### API 엔드포인트
- **POST /api/upload-image**
  - Request: `multipart/form-data` (file: File)
  - Response: `{ url: string }`
  - 파일 크기 제한: 5MB
  - 지원 형식: image/*

## 🧪 테스트

### 테스트 시나리오
1. **정상 업로드**
   - ✅ 이미지 선택 → 미리보기 표시
   - ✅ 업로드 버튼 클릭 → 진행률 표시
   - ✅ 업로드 완료 → URL 반환

2. **에러 케이스**
   - ✅ 5MB 초과 파일 → 에러 메시지
   - ✅ 이미지가 아닌 파일 → 에러 메시지
   - ✅ 네트워크 에러 → 재시도 안내

3. **UX 검증**
   - ✅ 미리보기 즉시 표시
   - ✅ 진행률 실시간 업데이트
   - ✅ 에러 메시지 명확히 표시

### 테스트 결과
- [ ] 단위 테스트: ✅ 통과 (ImageUploader 컴포넌트)
- [ ] 통합 테스트: ✅ 통과 (API 엔드포인트)
- [ ] E2E 테스트: ✅ 통과 (Playwright)

## 📸 스크린샷/데모
[이미지 업로드 전]
- 파일 선택 버튼만 표시

[이미지 선택 후]
- 미리보기 이미지 표시
- 업로드 버튼 활성화

[업로드 중]
- 진행률 바 표시
- "업로드 중..." 메시지

[업로드 완료]
- 심사관 목록에 이미지 반영
- 성공 토스트 메시지

## 🔗 관련 문서
- [[2025-09-20-Cloudflare-R2연동]] - 초기 설정
- [[2025-10-10-심사관관리기능]] - 메인 기능
- [[2025-10-16-이미지업로드-CORS문제]] - 트러블슈팅

## 📝 후속 작업
- [ ] 이미지 리사이징 자동화 (Sharp 라이브러리)
- [ ] 여러 이미지 동시 업로드
- [ ] 드래그 앤 드롭 지원

---

개발시작:: 2025-10-18T09:00:00
개발완료:: 2025-10-18T15:30:00
소요시간:: 6h30m
코드리뷰:: 완료
배포일시:: 2025-10-18T16:00:00
사용자피드백:: 긍정적 (업로드 전 확인 가능해서 좋다)
